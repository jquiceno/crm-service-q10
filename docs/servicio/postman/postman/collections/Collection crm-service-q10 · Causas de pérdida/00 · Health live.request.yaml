$kind: http-request
description: Sonda de proceso. No lleva tenant.
url: "{{baseUrl}}/health/live"
method: GET
scripts:
  - type: afterResponse
    code: |-
      pm.test('Responde 200', function () {
          pm.response.to.have.status(200);
      });
    language: text/javascript
order: 1000
