apiVersion: v1
kind: Service
metadata:
  name: crm-service
  namespace: crm-service
spec:
  selector:
    app: crm-service
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
