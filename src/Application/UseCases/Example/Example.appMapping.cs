namespace Application.UseCases.Example;

public static class ExampleMappingExtensions
{
    // TODO: Uncomment when a concrete Entity exists
    // public static ExampleEntity ToEntity(this ExampleInputDto dto) =>
    //     new(Guid.NewGuid(), dto.Name, dto.Email);

    // public static ExampleOutputDto ToDto(this ExampleEntity entity) =>
    //     new(entity.Id, entity.Name, entity.Email, entity.CreatedAtUtc);
}
