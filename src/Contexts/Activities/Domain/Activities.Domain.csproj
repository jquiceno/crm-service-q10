<Project Sdk="Microsoft.NET.Sdk">
  <ItemGroup>
    <ProjectReference Include="..\..\..\Shared\Domain\Shared.Domain.csproj" />
  </ItemGroup>

  <!-- Persistence rehydration only: the EF materialization interceptor needs the internal
       Activity.RestoreLegacyState hook (the legacy status bit pair and the type-dependent
       outcome char cannot be value converters — see ActivityConfiguration). -->
  <ItemGroup>
    <InternalsVisibleTo Include="Infrastructure" />
  </ItemGroup>
</Project>
