<Project Sdk="Microsoft.NET.Sdk">
  <ItemGroup>
    <ProjectReference Include="..\..\..\Shared\Domain\Shared.Domain.csproj" />
  </ItemGroup>

  <!-- Persistence rehydration only: the EF materialization interceptor needs the internal
       Activity.RestoreOutcomeType hook (the legacy outcome char depends on the activity type,
       so a value converter cannot rebuild it — see ActivityConfiguration). -->
  <ItemGroup>
    <InternalsVisibleTo Include="Infrastructure" />
  </ItemGroup>
</Project>
