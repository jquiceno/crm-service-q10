service_name      = "service-template"
github_repo       = "service-template-dotnet"
# TODO: set to the numeric GitHub repository ID of service-template-dotnet
# (init-service.sh replaces this when the service repo is created).
github_repo_id    = ""
environment       = "dev"
eks_cluster_names = ["q10-dev-eks"]
